# UnZip and ZipInfo environment options
# Note: Not all install of UnZip and ZipInfo supports the following options.
# And specify them in that case causes unzip/zipinfo to exit with code 10.
#export UNZIP='-O CP936'
#export ZIPINFO='-O CP936'
