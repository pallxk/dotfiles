# vi:ft=i3config

mode "connect" {
    # Start
    bindsym a exec 'for c in ~/bin/connect-a*; do "$c"; done'; mode "default"
    bindsym b exec 'for c in ~/bin/connect-b*; do "$c"; done'; mode "default"
    bindsym c exec 'for c in ~/bin/connect-c*; do "$c"; done'; mode "default"
    bindsym d exec 'for c in ~/bin/connect-d*; do "$c"; done'; mode "default"
    bindsym e exec 'for c in ~/bin/connect-e*; do "$c"; done'; mode "default"
    bindsym f exec 'for c in ~/bin/connect-f*; do "$c"; done'; mode "default"
    bindsym g exec 'for c in ~/bin/connect-g*; do "$c"; done'; mode "default"
    bindsym h exec 'for c in ~/bin/connect-h*; do "$c"; done'; mode "default"
    bindsym i exec 'for c in ~/bin/connect-i*; do "$c"; done'; mode "default"
    bindsym j exec 'for c in ~/bin/connect-j*; do "$c"; done'; mode "default"
    bindsym k exec 'for c in ~/bin/connect-k*; do "$c"; done'; mode "default"
    bindsym l exec 'for c in ~/bin/connect-l*; do "$c"; done'; mode "default"
    bindsym m exec 'for c in ~/bin/connect-m*; do "$c"; done'; mode "default"
    bindsym n exec 'for c in ~/bin/connect-n*; do "$c"; done'; mode "default"
    bindsym o exec 'for c in ~/bin/connect-o*; do "$c"; done'; mode "default"
    bindsym p exec 'for c in ~/bin/connect-p*; do "$c"; done'; mode "default"
    bindsym q exec 'for c in ~/bin/connect-q*; do "$c"; done'; mode "default"
    bindsym r exec 'for c in ~/bin/connect-r*; do "$c"; done'; mode "default"
    bindsym s exec 'for c in ~/bin/connect-s*; do "$c"; done'; mode "default"
    bindsym t exec 'for c in ~/bin/connect-t*; do "$c"; done'; mode "default"
    bindsym u exec 'for c in ~/bin/connect-u*; do "$c"; done'; mode "default"
    bindsym v exec 'for c in ~/bin/connect-v*; do "$c"; done'; mode "default"
    bindsym w exec 'for c in ~/bin/connect-w*; do "$c"; done'; mode "default"
    bindsym x exec 'for c in ~/bin/connect-x*; do "$c"; done'; mode "default"
    bindsym y exec 'for c in ~/bin/connect-y*; do "$c"; done'; mode "default"
    bindsym z exec 'for c in ~/bin/connect-z*; do "$c"; done'; mode "default"

    # Stop
    bindsym Shift+a exec 'for c in ~/bin/connect-a*; do "$c" stop; done'; mode "default"
    bindsym Shift+b exec 'for c in ~/bin/connect-b*; do "$c" stop; done'; mode "default"
    bindsym Shift+c exec 'for c in ~/bin/connect-c*; do "$c" stop; done'; mode "default"
    bindsym Shift+d exec 'for c in ~/bin/connect-d*; do "$c" stop; done'; mode "default"
    bindsym Shift+e exec 'for c in ~/bin/connect-e*; do "$c" stop; done'; mode "default"
    bindsym Shift+f exec 'for c in ~/bin/connect-f*; do "$c" stop; done'; mode "default"
    bindsym Shift+g exec 'for c in ~/bin/connect-g*; do "$c" stop; done'; mode "default"
    bindsym Shift+h exec 'for c in ~/bin/connect-h*; do "$c" stop; done'; mode "default"
    bindsym Shift+i exec 'for c in ~/bin/connect-i*; do "$c" stop; done'; mode "default"
    bindsym Shift+j exec 'for c in ~/bin/connect-j*; do "$c" stop; done'; mode "default"
    bindsym Shift+k exec 'for c in ~/bin/connect-k*; do "$c" stop; done'; mode "default"
    bindsym Shift+l exec 'for c in ~/bin/connect-l*; do "$c" stop; done'; mode "default"
    bindsym Shift+m exec 'for c in ~/bin/connect-m*; do "$c" stop; done'; mode "default"
    bindsym Shift+n exec 'for c in ~/bin/connect-n*; do "$c" stop; done'; mode "default"
    bindsym Shift+o exec 'for c in ~/bin/connect-o*; do "$c" stop; done'; mode "default"
    bindsym Shift+p exec 'for c in ~/bin/connect-p*; do "$c" stop; done'; mode "default"
    bindsym Shift+q exec 'for c in ~/bin/connect-q*; do "$c" stop; done'; mode "default"
    bindsym Shift+r exec 'for c in ~/bin/connect-r*; do "$c" stop; done'; mode "default"
    bindsym Shift+s exec 'for c in ~/bin/connect-s*; do "$c" stop; done'; mode "default"
    bindsym Shift+t exec 'for c in ~/bin/connect-t*; do "$c" stop; done'; mode "default"
    bindsym Shift+u exec 'for c in ~/bin/connect-u*; do "$c" stop; done'; mode "default"
    bindsym Shift+v exec 'for c in ~/bin/connect-v*; do "$c" stop; done'; mode "default"
    bindsym Shift+w exec 'for c in ~/bin/connect-w*; do "$c" stop; done'; mode "default"
    bindsym Shift+x exec 'for c in ~/bin/connect-x*; do "$c" stop; done'; mode "default"
    bindsym Shift+y exec 'for c in ~/bin/connect-y*; do "$c" stop; done'; mode "default"
    bindsym Shift+z exec 'for c in ~/bin/connect-z*; do "$c" stop; done'; mode "default"

    # Restart
    bindsym Ctrl+Shift+a exec 'for c in ~/bin/connect-a*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+b exec 'for c in ~/bin/connect-b*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+c exec 'for c in ~/bin/connect-c*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+d exec 'for c in ~/bin/connect-d*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+e exec 'for c in ~/bin/connect-e*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+f exec 'for c in ~/bin/connect-f*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+g exec 'for c in ~/bin/connect-g*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+h exec 'for c in ~/bin/connect-h*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+i exec 'for c in ~/bin/connect-i*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+j exec 'for c in ~/bin/connect-j*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+k exec 'for c in ~/bin/connect-k*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+l exec 'for c in ~/bin/connect-l*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+m exec 'for c in ~/bin/connect-m*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+n exec 'for c in ~/bin/connect-n*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+o exec 'for c in ~/bin/connect-o*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+p exec 'for c in ~/bin/connect-p*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+q exec 'for c in ~/bin/connect-q*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+r exec 'for c in ~/bin/connect-r*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+s exec 'for c in ~/bin/connect-s*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+t exec 'for c in ~/bin/connect-t*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+u exec 'for c in ~/bin/connect-u*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+v exec 'for c in ~/bin/connect-v*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+w exec 'for c in ~/bin/connect-w*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+x exec 'for c in ~/bin/connect-x*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+y exec 'for c in ~/bin/connect-y*; do "$c" stop; "$c"; done'; mode "default"
    bindsym Ctrl+Shift+z exec 'for c in ~/bin/connect-z*; do "$c" stop; "$c"; done'; mode "default"

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+c mode "default"
}
bindsym $mod+c mode "connect"
