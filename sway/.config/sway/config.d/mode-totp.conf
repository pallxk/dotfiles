# vi:ft=i3config

mode "totp" {
    bindsym a exec "TOTP_DIR=$totp_dir totp a | wtype -d 10 -"; mode "default"
    bindsym b exec "TOTP_DIR=$totp_dir totp b | wtype -d 10 -"; mode "default"
    bindsym c exec "TOTP_DIR=$totp_dir totp c | wtype -d 10 -"; mode "default"
    bindsym d exec "TOTP_DIR=$totp_dir totp d | wtype -d 10 -"; mode "default"
    bindsym e exec "TOTP_DIR=$totp_dir totp e | wtype -d 10 -"; mode "default"
    bindsym f exec "TOTP_DIR=$totp_dir totp f | wtype -d 10 -"; mode "default"
    bindsym g exec "TOTP_DIR=$totp_dir totp g | wtype -d 10 -"; mode "default"
    bindsym h exec "TOTP_DIR=$totp_dir totp h | wtype -d 10 -"; mode "default"
    bindsym i exec "TOTP_DIR=$totp_dir totp i | wtype -d 10 -"; mode "default"
    bindsym j exec "TOTP_DIR=$totp_dir totp j | wtype -d 10 -"; mode "default"
    bindsym k exec "TOTP_DIR=$totp_dir totp k | wtype -d 10 -"; mode "default"
    bindsym l exec "TOTP_DIR=$totp_dir totp l | wtype -d 10 -"; mode "default"
    bindsym m exec "TOTP_DIR=$totp_dir totp m | wtype -d 10 -"; mode "default"
    bindsym n exec "TOTP_DIR=$totp_dir totp n | wtype -d 10 -"; mode "default"
    bindsym o exec "TOTP_DIR=$totp_dir totp o | wtype -d 10 -"; mode "default"
    bindsym p exec "TOTP_DIR=$totp_dir totp p | wtype -d 10 -"; mode "default"
    bindsym q exec "TOTP_DIR=$totp_dir totp q | wtype -d 10 -"; mode "default"
    bindsym r exec "TOTP_DIR=$totp_dir totp r | wtype -d 10 -"; mode "default"
    bindsym s exec "stoken | wtype -d 10 -"; mode "default"
    bindsym t exec "TOTP_DIR=$totp_dir totp t | wtype -d 10 -"; mode "default"
    bindsym u exec "TOTP_DIR=$totp_dir totp u | wtype -d 10 -"; mode "default"
    bindsym v exec "TOTP_DIR=$totp_dir totp v | wtype -d 10 -"; mode "default"
    bindsym w exec "TOTP_DIR=$totp_dir totp w | wtype -d 10 -"; mode "default"
    bindsym x exec "TOTP_DIR=$totp_dir totp x | wtype -d 10 -"; mode "default"
    bindsym y exec "TOTP_DIR=$totp_dir totp y | wtype -d 10 -"; mode "default"
    bindsym z exec "TOTP_DIR=$totp_dir totp z | wtype -d 10 -"; mode "default"

    bindsym Mod1+a exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp a)"; mode "default"
    bindsym Mod1+b exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp b)"; mode "default"
    bindsym Mod1+c exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp c)"; mode "default"
    bindsym Mod1+d exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp d)"; mode "default"
    bindsym Mod1+e exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp e)"; mode "default"
    bindsym Mod1+f exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp f)"; mode "default"
    bindsym Mod1+g exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp g)"; mode "default"
    bindsym Mod1+h exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp h)"; mode "default"
    bindsym Mod1+i exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp i)"; mode "default"
    bindsym Mod1+j exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp j)"; mode "default"
    bindsym Mod1+k exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp k)"; mode "default"
    bindsym Mod1+l exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp l)"; mode "default"
    bindsym Mod1+m exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp m)"; mode "default"
    bindsym Mod1+n exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp n)"; mode "default"
    bindsym Mod1+o exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp o)"; mode "default"
    bindsym Mod1+p exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp p)"; mode "default"
    bindsym Mod1+q exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp q)"; mode "default"
    bindsym Mod1+r exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp r)"; mode "default"
    bindsym Mod1+s exec "notify-send --expire-time=30000 stoken $(stoken)"; mode "default"
    bindsym Mod1+t exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp t)"; mode "default"
    bindsym Mod1+u exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp u)"; mode "default"
    bindsym Mod1+v exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp v)"; mode "default"
    bindsym Mod1+w exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp w)"; mode "default"
    bindsym Mod1+x exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp x)"; mode "default"
    bindsym Mod1+y exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp y)"; mode "default"
    bindsym Mod1+z exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp z)"; mode "default"

    bindsym Shift+a exec "TOTP_DIR=$totp_dir totp A | wtype -d 10 -"; mode "default"
    bindsym Shift+b exec "TOTP_DIR=$totp_dir totp B | wtype -d 10 -"; mode "default"
    bindsym Shift+c exec "TOTP_DIR=$totp_dir totp C | wtype -d 10 -"; mode "default"
    bindsym Shift+d exec "TOTP_DIR=$totp_dir totp D | wtype -d 10 -"; mode "default"
    bindsym Shift+e exec "TOTP_DIR=$totp_dir totp E | wtype -d 10 -"; mode "default"
    bindsym Shift+f exec "TOTP_DIR=$totp_dir totp F | wtype -d 10 -"; mode "default"
    bindsym Shift+g exec "TOTP_DIR=$totp_dir totp G | wtype -d 10 -"; mode "default"
    bindsym Shift+h exec "TOTP_DIR=$totp_dir totp H | wtype -d 10 -"; mode "default"
    bindsym Shift+i exec "TOTP_DIR=$totp_dir totp I | wtype -d 10 -"; mode "default"
    bindsym Shift+j exec "TOTP_DIR=$totp_dir totp J | wtype -d 10 -"; mode "default"
    bindsym Shift+k exec "TOTP_DIR=$totp_dir totp K | wtype -d 10 -"; mode "default"
    bindsym Shift+l exec "TOTP_DIR=$totp_dir totp L | wtype -d 10 -"; mode "default"
    bindsym Shift+m exec "TOTP_DIR=$totp_dir totp M | wtype -d 10 -"; mode "default"
    bindsym Shift+n exec "TOTP_DIR=$totp_dir totp N | wtype -d 10 -"; mode "default"
    bindsym Shift+o exec "TOTP_DIR=$totp_dir totp O | wtype -d 10 -"; mode "default"
    bindsym Shift+p exec "TOTP_DIR=$totp_dir totp P | wtype -d 10 -"; mode "default"
    bindsym Shift+q exec "TOTP_DIR=$totp_dir totp Q | wtype -d 10 -"; mode "default"
    bindsym Shift+r exec "TOTP_DIR=$totp_dir totp R | wtype -d 10 -"; mode "default"
    bindsym Shift+s exec "stoken-gui"; mode "default"
    bindsym Shift+t exec "TOTP_DIR=$totp_dir totp T | wtype -d 10 -"; mode "default"
    bindsym Shift+u exec "TOTP_DIR=$totp_dir totp U | wtype -d 10 -"; mode "default"
    bindsym Shift+v exec "TOTP_DIR=$totp_dir totp V | wtype -d 10 -"; mode "default"
    bindsym Shift+w exec "TOTP_DIR=$totp_dir totp W | wtype -d 10 -"; mode "default"
    bindsym Shift+x exec "TOTP_DIR=$totp_dir totp X | wtype -d 10 -"; mode "default"
    bindsym Shift+y exec "TOTP_DIR=$totp_dir totp Y | wtype -d 10 -"; mode "default"
    bindsym Shift+z exec "TOTP_DIR=$totp_dir totp Z | wtype -d 10 -"; mode "default"

    bindsym Mod1+Shift+a exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp a)"; mode "default"
    bindsym Mod1+Shift+b exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp b)"; mode "default"
    bindsym Mod1+Shift+c exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp c)"; mode "default"
    bindsym Mod1+Shift+d exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp d)"; mode "default"
    bindsym Mod1+Shift+e exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp e)"; mode "default"
    bindsym Mod1+Shift+f exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp f)"; mode "default"
    bindsym Mod1+Shift+g exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp g)"; mode "default"
    bindsym Mod1+Shift+h exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp h)"; mode "default"
    bindsym Mod1+Shift+i exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp i)"; mode "default"
    bindsym Mod1+Shift+j exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp j)"; mode "default"
    bindsym Mod1+Shift+k exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp k)"; mode "default"
    bindsym Mod1+Shift+l exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp l)"; mode "default"
    bindsym Mod1+Shift+m exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp m)"; mode "default"
    bindsym Mod1+Shift+n exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp n)"; mode "default"
    bindsym Mod1+Shift+o exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp o)"; mode "default"
    bindsym Mod1+Shift+p exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp p)"; mode "default"
    bindsym Mod1+Shift+q exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp q)"; mode "default"
    bindsym Mod1+Shift+r exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp r)"; mode "default"
    bindsym Mod1+Shift+s exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp r)"; mode "default"
    bindsym Mod1+Shift+t exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp t)"; mode "default"
    bindsym Mod1+Shift+u exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp u)"; mode "default"
    bindsym Mod1+Shift+v exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp v)"; mode "default"
    bindsym Mod1+Shift+w exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp w)"; mode "default"
    bindsym Mod1+Shift+x exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp x)"; mode "default"
    bindsym Mod1+Shift+y exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp y)"; mode "default"
    bindsym Mod1+Shift+z exec "notify-send --expire-time=30000 totp $(TOTP_DIR=$totp_dir totp z)"; mode "default"

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+t mode "default"
    bindsym $mod+Ctrl+t mode "default"
}
bindsym $mod+t mode "totp"; set $totp_dir ~/.config/totp
